#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    int sum;
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    //int min=arr[0];
    for(int i=n-1;i>=0;i--){
        //sum=sum+arr[i];
        printf("%d",arr[i]);
      
    }
   // printf("%d ",max);
  //  printf("%d ",sum);
    return 0;
}
